version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: build
          command: ng build
      - run:
          name: Print the Current Time
          command: date
